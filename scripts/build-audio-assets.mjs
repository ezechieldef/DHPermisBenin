import { readFile, writeFile } from 'node:fs/promises';
import { resolve } from 'node:path';

const root = resolve(import.meta.dirname, '..');
const manifest = JSON.parse(await readFile(resolve(root, 'assets/audio/manifest.json'), 'utf8'));
const requireAsset = (file) => `require('../../assets/audio/${file}')`;
const webAsset = (file) => `'/assets/audio/${file}'`;
const courseLines = Object.entries(manifest.courses).map(([id, entries]) => `  ${id}: [${entries.map((entry) => requireAsset(entry.file)).join(', ')}],`);
const courseSegmentLines = Object.entries(manifest.courses).map(([id, entries]) => `  ${id}: [${entries.map((entry) => `{ source: ${requireAsset(entry.file)}, startLine: ${entry.start_line ?? 1}, endLine: ${entry.end_line ?? 99999} }`).join(', ')}],`);
const questionLines = Object.entries(manifest.questions).map(([id, entry]) => `  ${id}: ${requireAsset(entry.file)},`);
const optionLines = Object.entries(manifest.options).map(([id, entry]) => `  ${id}: ${requireAsset(entry.file)},`);
const output = `/* Généré par scripts/build-audio-assets.mjs — ne pas modifier manuellement. */\nimport type { AudioSource } from 'expo-audio';\n\nexport type CourseAudioSegment = { source: AudioSource; startLine: number; endLine: number };\nexport const COURSE_AUDIO: Record<number, AudioSource[]> = {\n${courseLines.join('\n')}\n};\nexport const COURSE_AUDIO_SEGMENTS: Record<number, CourseAudioSegment[]> = {\n${courseSegmentLines.join('\n')}\n};\nexport const QUESTION_AUDIO: Record<number, AudioSource> = {\n${questionLines.join('\n')}\n};\nexport const OPTION_AUDIO: Record<number, AudioSource> = {\n${optionLines.join('\n')}\n};\n\nexport function getCourseAudio(courseId: number) { return COURSE_AUDIO[courseId] ?? []; }\nexport function getCourseAudioSegments(courseId: number) { return COURSE_AUDIO_SEGMENTS[courseId] ?? []; }\nexport function getQuizAudio(questionId: number, optionIds: number[]) {\n  const sources: AudioSource[] = [];\n  const question = QUESTION_AUDIO[questionId];\n  if (question) sources.push(question);\n  optionIds.forEach((id) => { const option = OPTION_AUDIO[id]; if (option) sources.push(option); });\n  return sources;\n}\n`;
await writeFile(resolve(root, 'src/services/audio-assets.ts'), output);
const webCourseLines = Object.entries(manifest.courses).map(([id, entries]) => `  ${id}: [${entries.map((entry) => webAsset(entry.file)).join(', ')}],`);
const webCourseSegmentLines = Object.entries(manifest.courses).map(([id, entries]) => `  ${id}: [${entries.map((entry) => `{ source: ${webAsset(entry.file)}, startLine: ${entry.start_line ?? 1}, endLine: ${entry.end_line ?? 99999} }`).join(', ')}],`);
const webQuestionLines = Object.entries(manifest.questions).map(([id, entry]) => `  ${id}: ${webAsset(entry.file)},`);
const webOptionLines = Object.entries(manifest.options).map(([id, entry]) => `  ${id}: ${webAsset(entry.file)},`);
const webOutput = `/* Généré par scripts/build-audio-assets.mjs — URLs stables pour les packs PWA. */\nimport type { AudioSource } from 'expo-audio';\n\nexport type CourseAudioSegment = { source: AudioSource; startLine: number; endLine: number };\n\n// Metro réserve /assets/* à ses propres ressources en développement et y retire\n// le premier segment. L'export PWA, lui, publie les packs sous /assets/audio/*.\nconst webAudioUrl = (source: AudioSource): AudioSource => __DEV__ && typeof source === 'string' ? \`/assets\${source}\` : source;\n\nexport const COURSE_AUDIO: Record<number, AudioSource[]> = {\n${webCourseLines.join('\n')}\n};\nexport const COURSE_AUDIO_SEGMENTS: Record<number, CourseAudioSegment[]> = {\n${webCourseSegmentLines.join('\n')}\n};\nexport const QUESTION_AUDIO: Record<number, AudioSource> = {\n${webQuestionLines.join('\n')}\n};\nexport const OPTION_AUDIO: Record<number, AudioSource> = {\n${webOptionLines.join('\n')}\n};\n\nconst RESOLVED_COURSE_AUDIO = Object.fromEntries(Object.entries(COURSE_AUDIO).map(([id, sources]) => [id, sources.map(webAudioUrl)]));\nconst RESOLVED_COURSE_SEGMENTS = Object.fromEntries(Object.entries(COURSE_AUDIO_SEGMENTS).map(([id, segments]) => [id, segments.map((segment) => ({ ...segment, source: webAudioUrl(segment.source) }))]));\nconst RESOLVED_QUESTION_AUDIO = Object.fromEntries(Object.entries(QUESTION_AUDIO).map(([id, source]) => [id, webAudioUrl(source)]));\nconst RESOLVED_OPTION_AUDIO = Object.fromEntries(Object.entries(OPTION_AUDIO).map(([id, source]) => [id, webAudioUrl(source)]));\n\nexport function getCourseAudio(courseId: number) { return RESOLVED_COURSE_AUDIO[courseId] ?? []; }\nexport function getCourseAudioSegments(courseId: number) { return RESOLVED_COURSE_SEGMENTS[courseId] ?? []; }\nexport function getQuizAudio(questionId: number, optionIds: number[]) {\n  const sources: AudioSource[] = [];\n  const question = RESOLVED_QUESTION_AUDIO[questionId];\n  if (question) sources.push(question);\n  optionIds.forEach((id) => { const option = RESOLVED_OPTION_AUDIO[id]; if (option) sources.push(option); });\n  return sources;\n}\n`;
await writeFile(resolve(root, 'src/services/audio-assets.web.ts'), webOutput);
console.log(`Manifestes TypeScript natif + web : ${courseLines.length} cours, ${questionLines.length} questions, ${optionLines.length} options.`);
